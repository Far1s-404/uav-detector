dataset.py — Loads the drone images and their bounding-box labels and prepares them for training.

model.py — Defines the CNN architecture that learns to detect drones.

train_model.py — Trains the CNN using the dataset and saves the trained model.

test_model.py — Loads the trained model and tests its predictions on validation images.
